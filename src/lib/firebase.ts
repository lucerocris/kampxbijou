// Firebase has been removed as part of the migration to Supabase.
// Keep these exports as placeholders so existing pages compile.

export const app = null;
export const db = null;
export const storage = null;
export const analytics = null;
